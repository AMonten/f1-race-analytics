# Runtime dependencies. Kept in sync with pyproject.toml [project.dependencies].
# Install with: pip install -r requirements.txt
fastf1>=3.3
pandas>=2.0
numpy>=1.26
scipy>=1.11
plotly>=5.20
streamlit>=1.35

# Development / testing (install with: pip install -r requirements.txt -e ".[dev]")
pytest>=8.0
pytest-cov>=5.0
